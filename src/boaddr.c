int main(int argc, char* argv[ ])
{
     char a[30];  // in stack
     gets( a );  // when type in string longer than 32 char\ 
     // printf("strlen(a)=%d\n", strlen(a));
     printf("a=%s\n", a);
     //return lookup(a);
}

